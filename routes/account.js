var express = require('express');
var router = express.Router();


router.post('/signin', )
router.post('/signup', );
router.post('/checkid', );
// router.post('/findId', );

module.exports = router;
